#include <iostream>
#include <cstdlib>
#include <fstream>

int main(){
    //TEST FOR ENQUEUE
    // enqueue(18, p1_queue, &head[0],&tail[0]);
    // enqueue(19, p1_queue, &head[0],&tail[0]);
    // enqueue(20, p1_queue, &head[0],&tail[0]);
    // enqueue(21, p1_queue, &head[0],&tail[0]);
    // enqueue(22, p1_queue, &head[0],&tail[0]);
    // enqueue(23, p1_queue, &head[0],&tail[0]);
    // enqueue(24, p1_queue, &head[0],&tail[0]);
    // enqueue(25, p1_queue, &head[0],&tail[0]);
    // enqueue(26, p1_queue, &head[0],&tail[0]);
    // enqueue(27, p1_queue, &head[0],&tail[0]);
    // cout<<p1_queue[tail[0]]<<endl;
    // cout<<tail[0]<<endl;
    // cout<<head[0]<<endl;

    //TEST FOR DEQUEUE
    // dequeue(p1_queue,&head[0],&tail[0]);
    // dequeue(p1_queue,&head[0],&tail[0]);
    // dequeue(p1_queue,&head[0],&tail[0]);
    // dequeue(p1_queue,&head[0],&tail[0]);
    // dequeue(p1_queue,&head[0],&tail[0]);
    // dequeue(p1_queue,&head[0],&tail[0]);
    // dequeue(p1_queue,&head[0],&tail[0]);
    // dequeue(p1_queue,&head[0],&tail[0]);
    // dequeue(p1_queue,&head[0],&tail[0]);
    // dequeue(p1_queue,&head[0],&tail[0]);
    // dequeue(p1_queue,&head[0],&tail[0]);
    // cout<<p1_queue[head[0]]<<endl;
    // cout<<head[0]<<endl;


    //TEST FOR AUXILARY STATUS FUNCTION
    // enqueue(1, p1_queue, &head[0],&tail[0]);
    // enqueue(1, p1_queue, &head[0],&tail[0]);
    // enqueue(1, p1_queue, &head[0],&tail[0]);
    // enqueue(2, p2_queue, &head[1],&tail[1]);
    // enqueue(2, p2_queue, &head[1],&tail[1]);
    // enqueue(2, p2_queue, &head[1],&tail[1]);
    // enqueue(3, p3_queue, &head[2],&tail[2]);
    // enqueue(3, p3_queue, &head[2],&tail[2]);
    // enqueue(3, p3_queue, &head[2],&tail[2]);
    // enqueue(3, p3_queue, &head[2],&tail[2]);
    // current_status(p1_queue,p2_queue,p3_queue, head, tail);

    //TEST FOR RANDOM NUMBERS
    // srand(time(NULL));
    // cout<<random_data_generator()<< " " << random_priority_generator()<<endl;
    // cout<<random_data_generator()<< " " << random_priority_generator()<<endl;
    // cout<<random_data_generator()<< " " << random_priority_generator()<<endl;
    // cout<<random_data_generator()<< " " << random_priority_generator()<<endl;

    return 0;
}